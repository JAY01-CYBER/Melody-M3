<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using Material3 -->
    <style name="Theme.MelodyM3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main system colors -->
        <item name="colorPrimary">@android:color/holo_blue_dark</item>
        <item name="colorSecondary">@android:color/holo_blue_light</item>
        
        <!-- Status bar and navigation bar settings -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Support for components expecting MaterialComponents attributes -->
    <style name="Theme.MaterialComponents.DayNight.DarkActionBar" parent="Theme.Material3.DayNight">
        <!-- Automatically mapped -->
    </style>
</resources>
